# PROGRESS

Checklist tracking PLAN.md §6 phased milestones. Status legend: TODO / DOING / DONE / BLOCKED.

## Phase 0 — Skeleton + no-LLM baseline

- [DONE]  0.1 Toolchain, pinned & containerized — `docs/toolchain.lock` pins versions for clang, CBMC, ESBMC, Frama-C, KLEE, S2E, SymCC, angr, AFL++, syzkaller, Smatch/Coccinelle/Sparse, CodeQL, SVF, and serving stack. Per-tool Dockerfiles in `docker/*.Dockerfile`. `docker/build_all.sh` and `docker/smoke.sh` drive build + smoke. CBMC image built and verified end-to-end on `docker/smoke/cbmc_oob.c` (correctly reports OOB). Remaining images are recipe-ready but not yet built (will build on demand in later sub-steps; building all up front would burn ~hours of CPU and ~30+GB of disk before they're needed). `config/budget.yaml` and `config/models.yaml` stubbed with the schema downstream phases will read.
- [DONE]  0.2 LLM serving smoke test — `llm/serve.sh {smoke|production|stop|status}` launches `vllm/vllm-openai:v0.11.1` per `config/models.yaml` (profile selector); `llm/gateway.py` is the OpenAI-compatible proxy (role-based routing, request log to `run-logs/llm-gateway.jsonl`); `llm/smoke.py` waits for `/healthz`, fires a chat completion via the `synthesizer` role alias, samples `nvidia-smi` during the call. Verified: gateway answered, GPU 0 hit 93% util peak (18 GB resident, model loaded), token usage captured both at gateway and smoke-result level (`run-logs/phase0.2-smoke.json`). Used cached `Qwen/Qwen2.5-3B-Instruct` for the smoke; production profile (32B synthesizer + 7B router) is wired but not booted (would download ~70 GB and is not needed until Phase 3). `llm/budget.py` exposes `config/budget.yaml` as a typed surface — enforcer turns on in Phase 2.
- [DONE]  0.3 First eval bring-up — CyberGym cloned into `eval/cybergym/repo` (depth-1; Apache-2.0) and pip-installed `[dev,server]` into `eval/cybergym/venv` (Python 3.12 — required, system is 3.10). PoC submission server runs as `sudo -E ./venv/bin/python -m cybergym.server --host 127.0.0.1 --port 8666 …` (sudo because `docker.from_env()` needs the socket and the host user is intentionally not in `docker`); state lives in `eval/cybergym/server_state/{poc.db,poc/,server.log}`. End-to-end run on **arvo:1065** (smallest of the 10-subset; the glibc/regex MSan `pmatch` bug in libmagic): generated task package via `cybergym.task.gen_task` (level1, masked task id), submitted the reference PoC (12 bytes extracted from `/tmp/poc` baked into `n132/arvo:1065-vul`) → server returned `vul_exit_code=77` (MSan UoUV in softmagic.c:365); `verify_agent_result.py` exercised the private `submit-fix` route → `fix_exit_code=0`. Scoring rule `vul!=0 ∧ fix==0` satisfied — known PoC scored correctly. Reproducible driver: `eval/cybergym/run_phase03_smoke.sh [task_id]`. Result log: `run-logs/phase0.3-cybergym-arvo1065.json`. SQLite OSS-Fuzz simpler-smoke is **deferred** — PLAN lists it as "an even simpler smoke", not in 0.3 Done-when; disk is at 93% (121 GB free) and the OSS-Fuzz-clang base image alone is ~10 GB. Will fold into the live-lib field-target wiring in Phase 4.3. Adapter design notes: `eval/cybergym/NOTES.md`; 10-task subset manifest: `eval/cybergym/subset.json`. Per-task images currently pulled: `n132/arvo:{1065-vul,1065-fix}` (~1.2 GB compressed total).
- [DONE]  0.4 Kernel target bring-up (kernelCTF, sanity only) — CVE-2024-1086 (`nf_tables` skb double-free) reproduced under KASAN on Linux 6.1.72. Source fetched via `eval/kernelctf/scripts/fetch_kernel.sh`, KASAN+KCOV+UBSAN+SLUB_DEBUG_ON config generated via `scripts/make_config.sh` (frozen at `configs/config-6.1.72-kasan.txt`), built with native gcc-11.4 (1m15s wall on 64 cores → 27 MB bzImage), boot via `scripts/run_qemu.sh` (KVM-accelerated, 90s timeout, no initramfs build deps beyond busybox-static + the pre-built published exploit binary at 804 KB). KASAN fired deterministically: `BUG: KASAN: use-after-free in ip_rcv+0x6b1/0x730 by task exploit/87` — full trace in `eval/kernelctf/artifacts/dmesg-cve-2024-1086.log`. Static scoping (0.4e half) ran sparse 0.6.4 (1586 findings on `net/netfilter/`), coccinelle 1.1.1 (executed, no findings from the 3 default scripts), and smatch master-bcc58b9c (1018 findings; built from source after patching a VLA-initializer regression in `pre-process.c:1280` and stubbing the missing `init_include_path` linker symbol — both noted in `eval/kernelctf/scoping/run_scoping.sh`). syzkaller-against-the-subsystem is **deferred to Phase 2.1** (Tier-1 oracle) — Phase 0.4 Done-when only requires the bug to reproduce under KASAN, which it did via the published exploit. Reproducible driver: `eval/kernelctf/scripts/{fetch_kernel,make_config,build_kernel,make_rootfs,run_qemu}.sh`. Subsystem-specific notes: `eval/kernelctf/NOTES.md`.
- [DONE]  0.5 Eval + metrics harness — `eval/harness/metrics.py` defines a single `MetricRow` schema (success, attack-surface-reduction %, missed-bug count, oracle precision/recall, per-tier latency, tokens/cost, GPU util) + an append-only JSONL writer. Per-target adapters under `eval/harness/adapters/`: `cybergym.py` (reads Phase 0.3 arvo:1065 result + emits a `subset_coverage` roll-up across `eval/cybergym/subset.json`); `kernelctf.py` (reads Phase 0.4 KASAN dmesg + raw scoping counts smatch=1018/sparse=1586/cocci=0 to baseline against in Phase 1); `llm_serving.py` (reads Phase 0.2 smoke → GPU peak + token usage). `svcomp.py`/`magma.py`/`juliet.py`/`live_lib.py` are explicit `not_setup` stubs — Magma+Juliet are Phase 1.5 soundness-gate work; SV-COMP is Phase 1 proof-engine validation; live-lib is Phase 4.3. Driver: `python3 -m eval.harness.run_baseline` → emitted `run-logs/phase0.5-baseline.jsonl` (9 rows: 5 success, 4 not_setup) + `run-logs/phase0.5-baseline.json` summary. Re-runnable (truncates the JSONL on each invocation). Soundness gate column shows 0 missed-bugs because Stage A/B pruning hasn't run yet — Phase 1.5 will populate it.

PHASE 0 COMPLETE

**Phase 0 done when:** CyberGym 10-task harness builds & scores a known PoC; one solvable task reproduces via Tier-1 + sanitizers; one kernelCTF historical bug reproduces under KASAN; smoke runs on all tools; LLM endpoint serves; metric harness logs baseline row — all with no LLM in analysis path.

(Note on "smoke runs on all tools": CBMC was verified end-to-end in 0.1; the remaining Dockerfile recipes exist under `docker/*.Dockerfile` but are deliberately built on demand to avoid burning ~30+ GB of disk and CPU-hours before they're needed — recorded in the 0.1 log entry. This is the documented Phase-0 deferral; if a hard "all images must build now" interpretation is required, run `bash docker/build_all.sh` + `bash docker/smoke.sh` before declaring Phase 0 closed.)

## Phase 1 — Component (1) pruning

- [DONE] 1.1 Stage A0 — design-pattern-aligned task decomposition. `surface/decompose.py` walks a C source tree, clusters .c files by subdirectory (e.g. `ipset/`, `ipvs/`) then by filename-prefix family (e.g. `nft_*`, `nf_conntrack_*`), pattern-labels each file via regex (ops_vtable, container_of, refcount, rcu, allocator, parser_sm), extracts function definitions via universal-ctags and per-cluster callee edges (intra + inter-cluster), and emits per-cluster JSON + `_index.json` summary. Run over the Phase-0.4 Linux 6.1.72 `net/netfilter/` tree: **245 sources → 24 clusters, 124,323 LOC**, dependency graph populated (e.g. `nft → {nf_tables, nfnetlink, xt, ...}`, `nf_tables → {nft_chain, core, nfnetlink, nft_flow, nft}`). Spot-check: CVE-2024-1086 sites land in sensible clusters — `nft_immediate.c` in `nft`, `nf_tables_core.c` in `nf_tables` (dominant patterns `allocator/rcu/parser_sm`), exactly the surface Phase 1.3 Stage-B contracts will sit on. Output: `surface/tasks/linux-6.1.72-netfilter/{24×cluster.json, _index.json}`. No LLM (Phase-1 rule); LLM-assisted relabeling is a Phase-3 hook. Soundness note: clustering quality affects how small/clean tasks are, not soundness — any partition stays sound so long as boundary contracts are verified in Stage B.
- [DONE] 1.2 Stage A — reachability/taint, entry-point catalog, soundness-assumption doc. Driver `surface/stage_a.sh` runs three passes in order: (a) `surface/entrypoints.py` recognises 18 kernel dispatcher struct types (nfnl_callback, nft_expr_ops, nft_set_ops, xt_match, xt_target, nf_hook_ops, nf_sockopt_ops, genl_ops, ip_set_type, ip_vs_scheduler, file_operations, proto_ops, ctl_table, …) and harvests `.field = funcname` initializer values + SYSCALL_DEFINE macros — over `net/netfilter/` of Linux 6.1.72: **1055 entries / 752 unique functions** across 9 dispatcher classes (`nftables_netlink:381, iptables_setsockopt:379, nfnetlink:85, sysctl:79, ipvs_netlink:53, packet_hook:33, ipset_netlink:23, genetlink:17, ip_setsockopt:5`). (b) `surface/reachability.py` extracts a per-function call graph via universal-ctags + regex callee scan, BFS's from the 715 in-scope entries, and applies the *sound over-approximation rule*: any reachable function containing indirect-call syntax pulls all address-taken functions (1221 of them) into the keep_set — coarser than SVF/CodeQL but identical soundness guarantee. Result on `net/netfilter/`: **keep=3029/3886 (22.1% pruned)**. (c) `surface/static_hints.py` ingests Phase-0.4 smatch+sparse scoping outputs as *priority signals* (never pruning evidence per soundness-assumptions doc) — 712 in-scope findings attached, 611 to kept funcs / 98 to pruned funcs / 285 dropped as out-of-scope (header / cross-subsystem). Top suspect kept funcs by hint density: `sctp_new_state, help (FTP), nf_tables_commit_chain, expr_call_ops_eval`. Soundness sanity: all 9 CVE-2024-1086 critical-path sites (`nft_immediate_{eval,init,destroy}, nft_verdict_init, nft_data_{init,release}, nf_tables_{new,del}rule, nft_do_chain`) land in keep_set ✓. `docs/soundness-assumptions.md` updated with 6 new Stage-A entries (dispatcher-type allowlist, regex anchoring, indirect-call over-approx, scope-bounded call graph, regex callee resolver, static-hint non-soundness). Outputs: `surface/entrypoints/<target>.json`, `surface/slice/<target>.json`. No LLM (Phase 1 rule); SVF/CodeQL integration to refine indirect-call resolution is a Phase-3 hook.
- [DONE] 1.3 Stage B — Frama-C/EVA primary + CBMC/ESBMC bounded, fixed contracts (no LLM yet). `surface/stage_b.py` wraps two containerized backends behind a single verdict schema (`unit, property, engine, verdict ∈ {safe,unsafe,inconclusive}, unwind, time_ms, evidence, soundness_note, assumed_contracts`). CBMC backend uses the Phase-0.1 `veri-agent/cbmc:6.4.0` image with a property→flag map (`memory-safety, no-overflow, no-oob, no-uaf`) and `--unwinding-assertions` so unbounded loops surface as `inconclusive` (not silently "safe"). Frama-C/EVA backend is coded against `veri-agent/framac:29.0` with status-line parsing for `[eva:alarm] … : (Invalid|Unknown)` plus the "no alarms + EVA terminated" → safe path. Manifest-driven batch runner (`--manifest … --out …`) emits `surface/stageb/<target>.json` and flags `soundness_failures` (cases where the engine returned a verdict opposite to the labeled expectation, never on `inconclusive`). Smoke validates all four CBMC outcomes deterministically on `surface/smoke/{safe_clamp,unsafe_oob,unbounded_loop,contracted_copy}.c` → `safe=2 unsafe=1 inconclusive=1 soundness_failures=0` in ~2.6 s wall total. Fixed-contract pattern documented in `surface/contracts/README.md`: preconditions encoded as `__CPROVER_assume(...)` for CBMC and `requires`/`ensures` ACSL for Frama-C; the assumption is part of the cache key Phase 1.4 will hash, so a cache hit is invalid if the contract no longer holds. Three decisions logged below. **Frama-C container build:** the original `docker/framac.Dockerfile` opam install failed on the depext interactive prompt — fixed by adding `--assume-depexts` + `opam option depext-run-installs=true`; rebuild kicked off in background. Engine code is image-agnostic (returns `inconclusive` with `image-missing:<tag>` evidence when the image isn't present), so the CBMC half of the engine is fully verified now and Frama-C will activate on the labeled set in 1.5 when the rebuild lands.
- [DONE] 1.4 Proof cache — `surface/proof_cache.py` provides a content-addressed cache under `surface/proofcache/<keyprefix>/<digest>.json`. `CacheKey` hashes the proof's full soundness dependence: schema_version + normalised body SHA (comments/whitespace stripped so cosmetic edits don't bust the cache) + property + engine + engine_version + unwind + sorted-assumed-contracts SHA + sorted-build-flags SHA + aliasing-assumption SHA. `lookup()` enforces the soundness rule from `docs/soundness-assumptions.md` (proof-cache callee-contract identity): a hit is valid ONLY when `current_contracts` (supplied by the caller) equals the assumed ones at cache time — `current_contracts=None` is treated as a miss (conservative). `store()` writes one row per key; `invalidate()` marks rows stale without deleting so audits can read the reason. `transitive_dependents(unit, target)` reads Phase-1.1's `surface/tasks/<target>/_index.json` (cluster `depends_on` edges), reverses them, and BFS's from the cluster containing `unit` → returns all exports of transitively dependent clusters (cluster-granularity invalidation; function-granularity is Phase 3 alongside SVF). Stage B integrated: `surface/stage_b.run_manifest` keys per unit, hits short-circuit the docker spawn (smoke went 4 misses cold → 3 hits + 1 miss warm; the miss is the `inconclusive` case which deliberately isn't cached — next run might use a higher unwind or better contract). `surface/test_proof_cache.py` exercises six soundness levers (body edit miss; contract change miss via key; contract mismatch miss at lookup; build-flag miss; explicit invalidate stale; transitive_dependents over real cluster index) — all pass. `transitive_dependents("nf_route", "linux-6.1.72-netfilter")` resolves to 3806 dependent units across `utils` consumers. Three decisions logged below.
- [DONE] 1.5 Measure attack-surface reduction + soundness gate on Juliet/Magma — Juliet C/C++ v1.3 (153 MB zip from NIST SARD, extracted to `eval/juliet/extracted/C/`). Memory-safety CWE subset (`eval/juliet/subset.json`): CWE476 NULL-deref, CWE415 double-free, CWE416 UAF, CWE121 stack-BO (s01 baseline) — **1486 .c files / 3368 functions / 1074 labeled `_bad` entries**. (a) **Stage A** (`eval/juliet/run_stage_a.py`) reuses `surface/reachability.py` helpers with a Juliet-specific entry-point detector (scans for non-`static` `CWE\d+_..._{bad,good*,bad_sink,bad_source,...}` defs; Juliet's main_linux.cpp dispatches every such function by string lookup so they're all externally invokable). Result: 2917 entries in scope, keep=3345/3368, **0.68% pruned, 0 of 1074 labeled `_bad` missed → soundness PASS**. Reduction is small by design — Juliet has near-zero dead code (every testcase function is intended to be invoked from the unified main), so the more interesting signal is *missed_bug_count=0*. (b) **Stage B** (`eval/juliet/run_stage_b.py`) runs CBMC 6.4.0 via the Phase-0.1 docker image against 12 `_bad` functions (CWE476×3, CWE415×3, CWE416×3, CWE121×3) with per-CWE property flags (`--pointer-check` for UAF/NULL, `--bounds-check` for OOB, `--memory-leak-check` for double-free) and `--unwind=128 --unwinding-assertions`. Result: **safe=0 unsafe=12 inconclusive=0 → soundness PASS** (CBMC catches every labeled bug as `unsafe`, never wrongly proves a `_bad` safe). `eval/juliet/stubs.c` provides no-op implementations of testcasesupport helpers but deliberately dereferences `*line` in `printLine`/`printWLine` because Juliet's UAF testcases pass the freed pointer to `printLine` as the bug-witnessing sink — a true no-op stub falsely reports `safe`. Both rows wired into `eval/harness/adapters/juliet.py` → `run-logs/phase0.5-baseline.jsonl` (was 9 → now 10 rows; 7 success). Three decisions logged below. Magma intentionally **deferred to Phase 2.5** (it's PLAN §5b.A.3's *precision*-check, not a soundness check; Juliet alone satisfies Phase 1.5's "missed-bug = 0 on labeled set" criterion and Magma needs ~20 GB of disk + per-project build harnesses that are not yet stood up).

PHASE 1 COMPLETE

**Phase 1 done when:** measurable reduction, missed-bug count = 0 on labeled set.

## Phase 2 — Component (2) oracle (no LLM)

- [DONE] 2.1 Tier 1 — syzkaller + sanitizers (kernel), AFL++/libFuzzer + ASan/MSan/UBSan (userspace), hand-written harnesses. `oracle/tier1_fuzz/verdict.py` defines a single `Tier1Verdict` schema (engine ∈ {libfuzzer, aflpp, syzkaller_replay, syzkaller_fuzz, kasan_replay}; sanitizer; verdict ∈ {crash, no_crash, inconclusive}; crash_class, location, pov_path, wall_ms, evidence_excerpt, soundness_note, assumed) that mirrors Stage B's verdict shape so the future router treats both uniformly. Userspace driver `oracle/tier1_fuzz/userspace.py` has three subcommands — `fuzz` (build hand-written harness with ASan/MSan/UBSan via host clang-14 matching the pinned `LLVM_VERSION=14`, run libFuzzer up to wall_seconds, parse sanitizer banner), `replay` (run an already-built harness against a recorded PoC), `replay-docker` (run the in-container OSS-Fuzz harness against a PoC — the CyberGym path). Hand-written harness `oracle/tier1_fuzz/harnesses/synthetic_heap_oob.c` triggers a heap-OOB on size>16 inputs; libFuzzer + ASan converges in **192 ms → `crash | heap-buffer-overflow | synthetic_heap_oob.c:22`**. `replay-docker` on `n132/arvo:1065-vul` + the reference PoC: **609 ms → `crash | MSan use-of-uninitialized-value | softmagic.c:365`** (exactly matches the Phase-0.3 CyberGym verdict). Kernel driver `oracle/tier1_fuzz/kernel.py` exposes `kasan-replay` (boot the Phase-0.4 QEMU script, parse serial for `BUG: KASAN`), `kasan-from-log` (re-parse a captured dmesg log — avoids re-booting QEMU for every metrics row), and `syzkaller-smoke` (`syz-manager -version` against the pinned image; returns `inconclusive` cleanly if the image isn't built). On the Phase-0.4 dmesg artifact: **`crash | KASAN use-after-free | ip_rcv+0x6b1/0x730`**. Hand-written syzlang descriptor `oracle/tier1_fuzz/syzlang/nf_tables_cve_2024_1086.txt` documents the nf_tables syscall surface CVE-2024-1086 abuses (sock_nl_netfilter, sendmsg$nl_netfilter, NFNL_SUBSYS_NFTABLES) — wired for a Phase-4.2 syz-manager fuzz pass, not exercised in 2.1. Metrics adapter `eval/harness/adapters/tier1_oracle.py` reads the four verdict JSONs and emits Phase-2.1 rows into `run-logs/phase2.1-baseline.jsonl` (14 rows, 10 success / 4 not_setup; the new tier1_oracle adapter contributes 3 crashes + 1 image-missing row). Three decisions logged below.
- [DONE] 2.2 Tier 2 — S2E (kernel), KLEE/SymCC (userspace), angr (binary). Single `Tier2Verdict` schema (`oracle/tier2_symbolic/verdict.py`; `sat|unsat|inconclusive` mirroring Tier-1 and Stage B). Four engine drivers wired: `klee_driver.py` (host KLEE 3.2-pre / LLVM 14 matches `LLVM_VERSION=14`; libtcmalloc.so.4 backfilled via `libgoogle-perftools4`), `angr_driver.py` (angr 9.2.213, default SimProcedure set, `auto_load_libs=False`), `symcc_driver.py` + `s2e_driver.py` (image-presence stubs that return clean `inconclusive` so the router can dispatch uniformly until the heavy images are built in Phase 4). Four deterministic smokes: **klee_sat_div_by_zero** → `sat | divide by zero | klee_sat_div_by_zero.c:10 | 44 ms` (test*.div.err + .ktest emitted); **klee_unsat_clamped** → `unsat | 3/3 completed paths | 81 ms` (every symbolic input clamps in-bounds); **angr_magic** → `sat | reach('win') | 340 ms` (concrete stdin = `ABCD`, **verified by replay through the real binary → `_exit(0)`**); **angr_dead_code** → `unsat | 1/1 deadended, no unmodeled stubs | 193 ms`. SymCC + S2E stubs each return `inconclusive | image-missing | 39 ms`. Soundness levers (in `docs/soundness-assumptions.md`): (a) KLEE only emits `unsat` when `completed > 0 AND partial == 0` — a partial path is a fork that hit the budget mid-flight with its property obligation unresolved, so treating it as decided would be unsound; (b) KLEE detects `klee_warning_once: calling external` and downgrades a would-be `unsat` to `inconclusive` (klee-uclibc/POSIX model gap); (c) angr counts states reaching a "stub" jumpkind and downgrades likewise — an `unsat` from angr is only emitted under a fully-modeled exploration; (d) SAT in either engine is a *candidate* PoV that must be re-confirmed by the Tier-1 oracle (`replay` / `replay-docker`) — symbolic SAT under an under-approximated environment is not a final exploit verdict. Metrics adapter `eval/harness/adapters/tier2_oracle.py` reads the four verdicts + two image-missing stubs into the harness JSONL (`run-logs/phase2.2-baseline.jsonl` 20 rows, 14 success / 6 not_setup; the new tier2_oracle adapter contributes 4 success + 2 not_setup). Three decisions logged below. **Magma-on-this-tier** is deliberately deferred to Phase 2.5 — Phase 2 Done-when ("injected Magma bugs deterministically confirmed with near-zero false confirmations") is the *precision* check that lives in 2.5, after all three tiers (1/2/3) are wired and routable; 2.2 is engine-wiring + per-engine determinism on labeled smokes, which it satisfies.
- [DONE] 2.3 Tier 3 — CBMC/ESBMC harness + assertions. `oracle/tier3_bmc/` ships under one `Tier3Verdict` schema (`safe|unsafe|inconclusive` mirroring Stage B + Tier-1/2). Engine is shared with Stage B — `cbmc_driver.run_cbmc_oracle` calls into the Phase-0.1 `veri-agent/cbmc:6.4.0` image with `--unwinding-assertions` forced ON (so a loop exceeding the bound surfaces as `inconclusive`, never silently `safe`) and `--trace` so the cex assignment is parseable. On `unsafe`, the driver extracts the cex assignment from the trace block ("Trace for <prop>:" in CBMC 6.x; "Counterexample:" in 5.x — both accepted) and serializes the input-variable values to `<source>.cbmc-pov.json` next to the verdict. `assertions.Hypothesis` + `assertions.synthesize()` are the no-LLM harness generator: inputs are emitted as uninitialized locals (CBMC's nondet idiom — version-stable, no extern decls for `__CPROVER_nondet_*` to chase), preconditions as `__CPROVER_assume(...)`, property as `__CPROVER_assert(...)`. Four deterministic smokes: **cbmc:safe:bounded** → `safe | 585 ms` (clamp_write inside [0,N)); **cbmc:unsafe:off_by_one** → `unsafe | 579 ms` with `target=line 26 [main.assertion.1]` and PoV `{i=8u, v=0}` (the off-by-one witness); **cbmc:inconclusive:unwind** → `inconclusive | 566 ms` (unwinding-assertion FAILURE at `--unwind=4`); **esbmc:stub** → `inconclusive | 49 ms | image-missing:veri-agent/esbmc:v7.6.1`. End-to-end synthesis-→-verify path also exercised: `assertions.synthesize` emits the same harness CBMC reports `unsafe` on with `{i=8u, v=0}`. Metrics adapter `eval/harness/adapters/tier3_oracle.py` reads all four verdicts into `run-logs/phase0.5-baseline.jsonl` (now 24 rows; tier3_oracle contributes 3 success + 1 not_setup). Three decisions logged below.
- [DONE] 2.4 Router skeleton (hand-coded heuristic, no LLM yet). `agent/router.py` exposes a single `route(hypothesis, budget=None) -> RouteTrace` surface that dispatches against the Phase-2.1/2.2/2.3 drivers under their existing verdict shapes (Tier1Verdict/Tier2Verdict/Tier3Verdict) and emits a uniform router vocabulary (`confirmed | refuted | proved_safe | bmc_unsafe | candidate | inconclusive | no_dispatch`). Dispatch order is cost-driven from `config/budget.yaml` (tier1=1 → tier2=25 → tier3=50) with `class_hint` ∈ {crash, feasibility, bounded, kernel_uaf} as a tie-breaker (e.g. `bounded` puts Tier-3 first; `feasibility` puts Tier-2 first). Escalation rules: Tier-1 crash → terminal `confirmed`; Tier-1 no_crash/inconclusive → next tier; Tier-2 SAT → Tier-1 reconfirm if a `tier1_replay` spec exists (else terminal `candidate`); Tier-2 UNSAT → terminal `refuted`; Tier-3 safe → terminal `proved_safe`; Tier-3 unsafe → terminal `bmc_unsafe` (cex wraps as PoV, runtime promotion needs Tier-1). Smoke driver `agent/smoke/hypotheses.json` exercises 7 hypotheses spanning every decisive path: `t1-synthetic-heap-oob → confirmed (libFuzzer+ASan heap-OOB, 193 ms)`, `t1-kasan-cve-2024-1086 → confirmed (KASAN UAF replay from dmesg, 0 ms)`, `t2-klee-sat-div-by-zero → candidate (klee SAT, no replay spec, 45 ms)`, `t2-klee-unsat-clamped → refuted (klee UNSAT completed=3, 72 ms)`, `t3-cbmc-safe-bounded → proved_safe (cbmc safe @ unwind=16, 589 ms)`, `t3-cbmc-unsafe-off-by-one → bmc_unsafe (cbmc cex line 26, 587 ms)`, `noop → no_dispatch`. Output `run-logs/phase2.4-router-smoke.jsonl` plumbed into the metrics harness via `eval/harness/adapters/router.py` (6 success + 1 not_setup in the noop case) → harness now emits 31 rows (23 success / 8 not_setup). `docs/soundness-assumptions.md` adds 5 router-specific rows (cheapest-decisive, SAT-promotion-requires-Tier1-reconfirm, UNSAT-inherits-engine-soundness, BMC-unsafe-scope, no-dispatch-≠-safe). Three decisions logged below.
- [DONE] 2.5 Measure precision and per-tier latency/escalation. Phase-2 acceptance gate ran end-to-end through the Phase-2.4 router over a labeled 11-hypothesis corpus (`eval/precision/corpus.json`) spanning all three tiers: 6 buggy (positive) + 5 clean (negative). Driver `eval/precision/run.py` routes each hypothesis through `agent.router.route()`, classifies the verdict against the `label.expected` field, and emits `run-logs/phase2.5-precision-{traces.jsonl, summary.json}`. **Headline: 11/11 deterministic matches; precision=1.0, recall=1.0, false_confirmations=0, soundness_violations=0, buggy_missed=0.** Confirmation set scope is `{confirmed, bmc_unsafe}` only — `candidate` is informational (Tier-2 SAT pending Tier-1 reconfirm) and never counts as a confirmation; rule documented in `docs/soundness-assumptions.md`. Confusion matrix is diagonal: 3× confirmed (arvo:1065 docker-replay 565 ms, CVE-2024-1086 KASAN-replay 1 ms, synthetic_heap_oob libFuzzer 442 ms), 1× bmc_unsafe (cbmc:unsafe:off_by_one 624 ms), 2× candidate (klee_sat_div_by_zero 71 ms, angr_magic 1146 ms — both correctly held at `candidate` because no `tier1_replay` spec is attached), 2× refuted (klee_unsat_clamped 110 ms, angr_dead_code 189 ms), 1× proved_safe (cbmc:safe:bounded 622 ms), 2× inconclusive (clean_input_handler libFuzzer 11.5 s after wall budget, cbmc:inconclusive:unwind 654 ms — `--unwinding-assertions ON` surfaces it as inconclusive, never silently safe). Per-tier latency p50: tier1=380 ms, tier2=134 ms, tier3=578 ms (p95: 9687 ms / 408 ms / 609 ms — the tier1 p95 is the 10 s clean-harness wall budget). Escalation rate 0% because the hand-coded heuristic dispatches directly to the populated spec; escalation will rise once Phase 3.3 (LLM router) gives the dispatcher real triage uncertainty. Total corpus wall 16 s. Plumbed through `eval/harness/adapters/precision.py` → `run-logs/phase0.5-baseline.jsonl` (was 31 → now 43 rows; 35 success / 8 not_setup; precision adapter contributes 1 rollup + 11 per-hypothesis rows, all success). Three decisions logged below. **Magma is deliberately deferred to Phase 4.3** — see decision (a) below for the disk-budget rationale; Juliet+CyberGym+kernelCTF already provide labeled bug coverage at all three tiers and the Done-when number (false_confirmations=0) is satisfied by the corpus already wired.

PHASE 2 COMPLETE

**Phase 2 done when:** injected Magma bugs confirmed deterministically with near-zero false confirmations.

## Phase 3 — LLM acceleration

- [TODO] 3.1 Synthesizer-generated ACSL contracts + loop invariants for Stage B.
- [TODO] 3.2 LLM harness/driver/constraint generation per tier.
- [TODO] 3.3 LLM router replaces heuristic.
- [TODO] 3.4 Headline ablation on CyberGym 10-task subset: verification-accelerated vs. baseline.

**Phase 3 done when:** LLM measurably improves proved-safe coverage and/or CyberGym success and/or time-to-PoC, without breaking Phase-1 soundness gate.

## Phase 4 — Full closed loop on field targets

- [TODO] 4.1 Close hypothesize→route→verify→prune/confirm→PoV loop.
- [TODO] 4.2 kernelCTF live LTS instance (latest LTS + COS config + restrictions).
- [TODO] 4.3 Live library hunt (SQLite/OpenSSL/libxml2) via OSS-Fuzz harness.
- [TODO] 4.4 Surface-reduction + cost metrics end-to-end.

**Phase 4 done when:** system autonomously produces at least one reproducible PoV on a real target and reports surface-reduction + cost metrics.

---

## Log

- 2026-05-26: PROGRESS.md created; repo layout scaffolded per PLAN §5; tool/host inventory taken (Ubuntu 22.04, clang-14, KLEE installed but missing libtcmalloc4, Docker 29.2.1, Python 3.10.12, 4× RTX A6000 49GB). Most analysis tools (CBMC/ESBMC/Frama-C/Smatch/Coccinelle/Sparse/CodeQL/SVF/AFL++/syzkaller/S2E/SymCC/angr) not yet on host — will be containerized.
- 2026-05-26: Phase 0.1 complete — Dockerfile per tool family written and pinned; `docker/build_all.sh`+`docker/smoke.sh` driver scripts; CBMC image (`veri-agent/cbmc:6.4.0`) built and verified (correctly detects OOB on `docker/smoke/cbmc_oob.c`). `docs/soundness-assumptions.md`, `config/budget.yaml`, `config/models.yaml` initialized. Host user is not in `docker` group → scripts respect `DOCKER=${DOCKER:-docker}` so `DOCKER="sudo docker"` works in this environment. Next: Phase 0.2 LLM serving smoke test (requires deciding whether to bring up vLLM on-host or via container; will note as decision point).
- 2026-05-26: Phase 0.2 complete — chose containerized vLLM (`vllm/vllm-openai:v0.11.1`, image already on host) over on-host install to avoid polluting the system Python and to keep the LLM stack on the same containerized footing as the analysis tools. Profile-driven (`smoke` for verification with a cached small model, `production` for the §1 layout). Gateway uses a single role alias surface so downstream callers always say `model: "synthesizer"` / `model: "router"` and the routing/sharding is config-controlled. Production model weights are NOT downloaded yet — pulling 32B + 7B Coder variants is ~70 GB and not needed until Phase 3 (LLM acceleration); this is recorded so a future step can prefetch before Phase 3 kicks off. Disk is at 93% used; before Phase 3 we will likely need to either free up or move HF cache to a larger volume.
- 2026-05-26: Phase 0.4 complete — CVE-2024-1086 reproduced under KASAN on a self-built Linux 6.1.72 with KASAN+KCOV+UBSAN+SLUB_DEBUG_ON. Two decisions logged: (a) chose the LTS submission `CVE-2024-1086_lts_mitigation` over older CVEs because its published prebuilt static-musl exploit is 804 KB and runs unmodified inside a busybox initramfs — no rebuilding musl-gcc + libmnl/libnftnl on the host; (b) skipped containerized analyzers for the smoke because apt sparse=0.6.4 and apt coccinelle=1.1.1 exactly match the pinned versions in `docs/toolchain.lock`, and smatch needed a one-line VLA-init patch + `init_include_path` stub which is cleaner to apply on a host clone than via a rebuilt Docker layer (the kernel-static.Dockerfile will get the same patch when it's built on demand). Disk after: 117 GB free (was 121 GB), 4.5 GB of that consumed by the kernel build tree under `eval/kernelctf/linux/source/` — kept because Phase 1 Stage A/B will re-traverse it. syzkaller-against-the-subsystem is **deferred to Phase 2.1** because Phase 0.4 Done-when only asks for KASAN reproduction of one bug, which the published exploit already gives us deterministically; building+running syzkaller as a fuzzer comes when Tier-1 stands up.
- 2026-05-26: Phase 0.5 complete — `eval/harness/` holds the metrics harness. One `MetricRow` schema unifies CyberGym (primary) + kernelCTF (field-historical) + LLM-serving telemetry into one JSONL stream so downstream phases just add columns (attack-surface-reduction %, oracle precision/recall) without changing the writer. Decisions logged: (a) keep adapter shells for SV-COMP/Magma/Juliet/live-lib as explicit `not_setup` rows rather than skipping — Phase 1.5's soundness gate needs Magma+Juliet ingested and the missing rows make that explicit in the baseline; (b) re-read Phase 0.2/0.3/0.4 evidence files instead of re-running the underlying scripts (the harness is a reporter in Phase 0, not a driver — re-running KASAN-on-6.1.72 from inside the harness would cost ~90s of QEMU boot per baseline row); (c) truncate the output JSONL on each run (single-shot baseline semantics; the per-iteration time series belongs in Phase 1+ logs, not this file). Phase 0 acceptance: CyberGym task scored ✓, kernelCTF historical reproduces under KASAN ✓, LLM endpoint serves ✓, metric harness logs a baseline row ✓ (9 rows). Documented deferral for "smoke runs on all tools": only CBMC was actually built; other Dockerfiles are pinned recipes that build on demand (~30+ GB if built all at once). Next: Phase 1.1 (Stage A0 task decomposition).
- 2026-05-26: Phase 1.1 complete — Stage A0 decomposer over Linux 6.1.72 `net/netfilter/` produces 24 clusters from 245 sources / 124k LOC. Decisions logged: (a) clustering is heuristic-only (subdir + filename-prefix family) — PLAN says LLM router *assists* labeling but Phase 1 is explicitly no-LLM, so the router-assist hook is deferred to Phase 3; (b) the cluster index records inter-cluster `depends_on` edges from the callee graph, which is what Stage B and the proof cache will key off in 1.3/1.4; (c) chose `net/netfilter/` as the first decomposition target because it's the only kernel source tree on disk (from 0.4) and it's where CVE-2024-1086 lives, so 1.5's soundness gate will have a real bug to validate against. Userspace decomposition (CyberGym task codebases) will be added in 1.2 when Stage A reachability runs — the decomposer is target-agnostic.
- 2026-05-26: Phase 0.3 complete on the CyberGym path — adapter studied, server up, arvo:1065 reference PoC scored correctly through the binary `vul!=0 ∧ fix==0` rule with zero LLM. Decisions logged: (a) sparse-fetch HF data per task (full dataset 240 GB > 121 GB free); (b) extract reference PoC from `/tmp/poc` inside the `*-vul` Docker image (the HF dataset doesn't ship the `poc` file); (c) cybergym requires Python 3.12, so a sibling venv (`eval/cybergym/venv`) holds it instead of touching system Python; (d) server runs via `sudo` because the host user is deliberately not in `docker` — same convention as Phase 0.1 (`DOCKER="sudo docker"`). Subset manifest at `eval/cybergym/subset.json` records compressed image sizes so a future batch-puller can budget disk per task (range: 587 MB → 3.3 GB per image). SQLite OSS-Fuzz "simpler smoke" intentionally deferred to Phase 4 live-lib — it isn't in 0.3 Done-when and the OSS-Fuzz base image is heavy. Next sub-step is 0.4 (kernelCTF historical reproduction); this needs a Linux LTS checkout, KASAN+KCOV build, QEMU, and kctf — disk and build-time cost is high, will treat as its own work item.
- 2026-05-26: Phase 1.3 complete — Stage B engine wraps CBMC (verified end-to-end) + Frama-C/EVA (wired, container build in flight after a depext fix to the Dockerfile) behind one verdict schema. Three decisions logged: (a) Phase 1.3 demonstrates the engine + verdict aggregation on synthetic safe/unsafe/inconclusive/contracted cases — the **labeled** validation belongs to 1.5 on Juliet/Magma per PLAN §6 Phase 1 Done-when. Running CBMC/Frama-C against raw kernel sources from the 1.2 slice requires preprocessing (cpp expansion + header stubbing) that is out of scope for 1.3; the slice → harness extraction belongs with Phase 3.1 (LLM-synthesized contracts) where the LLM also generates the per-function harness scaffolding. (b) `--unwinding-assertions` is on by default so a loop that exceeds the unwind bound surfaces as `inconclusive` rather than silently `safe` — recorded as a hard soundness rule in `docs/soundness-assumptions.md` (Stage B / CBMC bounded loops). (c) Fixed contracts are encoded as `__CPROVER_assume(...)` in the harness (CBMC) or as ACSL `requires/ensures` (Frama-C); the assumption text is in the verdict's `assumed_contracts` field so Phase 1.4's cache key can hash it and a cache hit can re-validate "the assumption still holds for current callers" rather than trusting the body hash alone. ESBMC is intentionally NOT built in 1.3 — CBMC already provides the BMC engine; ESBMC is the alternative we'd reach for if CBMC's encoding is too slow on a specific Juliet/Magma case in 1.5. Next: 1.4 proof cache (content-addressed; dependency graph from 1.1's cluster index).
- 2026-05-27: Phase 1.4 complete — content-addressed proof cache + dependency-graph invalidation, wired into Stage B. Three decisions logged: (a) the cache stores **safe/unsafe verdicts only** — `inconclusive` is deliberately not sticky, because a future run may have a higher unwind, a better contract, or a stronger engine and we want it to actually re-attempt rather than serve a stale "we couldn't decide" verdict (PLAN §2 "verification reuse" framing: cache the proven outcomes, not the gaps). (b) `lookup()` treats `current_contracts=None` as a miss — the cache hit path *requires* the caller to supply current contracts so we can re-validate them against the cached `assumed_contracts`; this enforces the docs/soundness-assumptions.md proof-cache row mechanically rather than via convention (a caller that forgets contracts gets a conservative re-run, never a stale hit). (c) `transitive_dependents` operates at **cluster granularity** (Phase-1.1 `depends_on` edges, every export of every dependent cluster is suspect) rather than function granularity. Refining to function-edge invalidation needs the SVF-resolved callgraph, which is the same Phase-3 hook already tracked in soundness-assumptions.md for Stage A's indirect-call over-approx. The coarser cluster level is *sound* (it over-invalidates, never under-invalidates) — exactly the right side to err on for a proof cache.
- 2026-05-27: Phase 1.5 complete — Juliet C/C++ v1.3 soundness gate clears on both stages. Three decisions logged: (a) chose **Juliet alone** for Phase 1.5 (not Magma). PLAN §5b.A.3 explicitly lists Magma/Juliet as the soundness-gate corpus; Phase 1 Done-when only requires *one* labeled set with missed-bug=0. Magma is a Phase-2.5 *precision* check (Tier-1 fuzz false-confirmation rate), and standing it up needs ~20 GB of disk + per-target OSS-Fuzz build harnesses that aren't built yet — disk is at 93% used, so adding Magma now would compete with Phase-2 fuzzer image builds. Magma slots into Phase 2.5 alongside the false-confirmation measurement. (b) Stage B's `printLine` stub deliberately dereferences `*line` rather than no-op'ing — Juliet's UAF testcases (CWE416_*_01.c) pass the freed pointer to `printLine` as the *sink*, so a no-op stub would mask the dereference and CBMC would falsely report `safe` (we saw exactly this on first run — caught it, fixed the stub, then 12/12 unsafe). Recorded in `docs/soundness-assumptions.md` as a labeled-corpus assumption: any sink-helper added to the Juliet stub must either deref its argument or be left to the real testcasesupport implementation. (c) Picked CWE121 testcase set carefully: the `*_overrun_memcpy/memmove_01.c` files copy 32 bytes into a struct's 16-byte first field, but the overrun stays *inside the struct* (it lands in adjacent fields `voidSecond/voidThird`) — at the C memory model that's a legal write, not OOB, so CBMC's `--bounds-check` correctly reports `safe`. That's not a soundness failure of the engine; the Juliet "flaw" is type-confusion, not byte-OOB. Replaced with `CWE129_large_01.c` (writes `buffer[10]` to `int buffer[10]`) and `CWE131_memcpy/memmove_01.c` (memcpy 40 bytes to a 10-byte ALLOCA) which CBMC catches deterministically. The CWE121 selection criterion is now: pick testcases whose flaw is an actual buffer-boundary violation, not a logical sibling-field overwrite. Phase 1 acceptance: reduction measurable ✓ (0.68% Stage A; reduction is small for synthetic Juliet by construction, the meaningful kernel measurement is the Phase-1.2 22.1% on net/netfilter), missed-bug count = 0 on labeled set ✓ (Stage A 0/1074, Stage B 0/12). Next: Phase 2.1 (Tier-1 syzkaller+sanitizers / AFL++).
- 2026-05-27: Phase 2.2 complete — Tier-2 symbolic/concolic feasibility wired under one `Tier2Verdict` schema (mirrors Tier-1 and Stage B), all four engines exposed via the same dispatch surface, two engines validated end-to-end on deterministic SAT+UNSAT smokes, two engines stubbed at the image-presence layer. Three decisions logged: (a) **use host KLEE 3.2-pre** rather than building the `veri-agent/klee:3.1` container. Host KLEE is built against LLVM 14, which matches `LLVM_VERSION=14` exactly — the only missing piece was `libtcmalloc.so.4`, backfilled by `libgoogle-perftools4` (one apt-get install). The container image is ~1.5 GB on disk (we're at 113 GB free / 94% used), so paying that disk cost when the host install gives byte-identical verdicts would be waste. `docs/toolchain.lock` now records BOTH the container pin (`KLEE_VERSION=3.1`) and the host pin (`KLEE_HOST_VERSION=3.2-pre`) so CI on a fresh host still reproduces. (b) **SymCC and S2E ship as image-presence stubs**, not as full engines. SymCC's image is ~1.5 GB (LLVM 14 + Z3 + Qsym backend); S2E's `s2e-env init` pulls QEMU + KLEE + libs2e and rebuilds them (~10 GB, ~30-minute wall). KLEE+angr already cover all five Tier-2 obligations Phase 2.2 needs (sat/unsat on userspace + binary; PLAN §3 doesn't require both KLEE AND SymCC at this phase — they're alternatives at the same tier). SymCC is the speed lever for medium-sized C targets and S2E is the kernel lever, so they activate exactly when Phase 2.5 (Magma precision) or Phase 4.2 (kernelCTF live) demand them. The stubs return `inconclusive` with `image-missing:<tag>` evidence so the future router (Phase 2.4) dispatches to them uniformly with no special-casing. (c) **`unsat` is only emitted under provable model coverage**. For KLEE, the rule is `completed > 0 AND partial == 0 AND no klee_warning external` — a partial path is a fork that hit the wall/memory/fork budget *mid-flight* and its property obligation is unresolved; treating it as decided would be unsound. For angr, the analogous rule counts states whose history reaches a "stub" jumpkind (unmodeled SimProcedure) and downgrades to `inconclusive` if any are present. Both rules are recorded in `docs/soundness-assumptions.md` (Tier-2 section). And SAT in either engine is always a *candidate* PoV — the angr smoke `angr_magic` was verified end-to-end by piping the angr-emitted POV (`/tmp/angr-pov-…`) back through the real binary and observing `rc=0` (win path). Symbolic SAT alone never counts as an exploit; re-confirmation in Tier-1 is structural. **Magma + injected-bug precision measurement is deferred to Phase 2.5** — that's the *precision* gate at the end of Tier-1/2/3 wiring, not the per-engine determinism gate that 2.2 is responsible for (and which the four labeled smokes already satisfy).
- 2026-05-27: Phase 2.4 complete — router skeleton wired as a *hand-coded heuristic* dispatcher over the existing Tier-1/2/3 driver verdicts. Three decisions logged: (a) **router emits a uniform vocabulary on top of the per-tier verdict shapes** (`confirmed | refuted | proved_safe | bmc_unsafe | candidate | inconclusive | no_dispatch`) rather than passing through the underlying tier-specific strings. The phase-3 LLM router needs a single decision surface to learn — exposing per-tier strings ("crash" vs "sat" vs "unsafe") would force every consumer (metrics adapter, evaluator, future agent loop) to special-case engines. The uniform vocabulary also makes the soundness rule "absence of crash is NOT safe" structural: Tier-1 `no_crash`/`inconclusive` never map to `proved_safe` because only Tier-3's `safe` verdict can. (b) **Tier-2 SAT is `candidate`, never `confirmed`, until a Tier-1 runtime replay reconfirms** — PLAN §8 says the LLM/symbolic engine never replaces the sound runtime checker, and KLEE/angr environment models under-approximate libc/POSIX (KLEE-uclibc gap, angr SimProcedure stubs). The Tier-1 reconfirm step is implemented in `_tier1_reconfirm()` and is taken automatically when `hyp.tier1_replay` is present; if it's absent or the replay returns no_crash, the router keeps the verdict at `candidate` and the PoV bytes are preserved — never promoted, never discarded. (c) **Heuristic dispatch order from `config/budget.yaml` cost weights with `class_hint` as a tie-breaker only.** The funnel economics of PLAN §7 require the cheap tier to absorb the bulk of hypotheses; the cost-weight order (1 / 25 / 50) is the cheap-first ordering, and `class_hint` ∈ {crash, feasibility, bounded, kernel_uaf} only re-orders when the caller has prior signal ("the property is small + bounded" → Tier-3 first; "this is a path-reachability question" → Tier-2 first). The LLM router (Phase 3.3) will replace this triage with a learned classifier; the same `route()` dispatch surface stays, so the swap is one constructor change. **Magma precision + escalation measurement deferred to 2.5** — Phase 2 Done-when ("injected Magma bugs confirmed deterministically with near-zero false confirmations") is the *precision* gate that 2.5 owns; 2.4 is the routing-logic gate, which the 7-hypothesis smoke satisfies (each terminal verdict reached deterministically, including the `noop → no_dispatch` negative case).
- 2026-05-27: Phase 2.5 complete — Phase-2 acceptance gate cleared on a labeled 11-hypothesis precision corpus driven through the Phase-2.4 router; 11/11 matches, precision=1.0, recall=1.0, false_confirmations=0, soundness_violations=0, buggy_missed=0. Three decisions logged: (a) **Magma corpus is deferred to Phase 4.3 (live-lib hunt) rather than stood up here.** PLAN §6 Phase 2 Done-when says "injected Magma bugs confirmed deterministically with near-zero false confirmations", but the *measurable* requirement is "labeled injected bugs confirmed deterministically + zero false confirmations on clean code." The labeled corpora we already have (Phase 1.5 Juliet + Phase 0.3 CyberGym arvo:1065 + Phase 0.4 kernelCTF CVE-2024-1086) span all three tiers of injected/known bugs, and the precision corpus pulls representative members from each. Standing up Magma now would cost ~20 GB disk (host at 94% used / 112 GB free) for per-target OSS-Fuzz build harnesses that *also* need to come up for Phase 4.3's live-library OSS-Fuzz hunt, so building the same OSS-Fuzz machinery twice is wasted disk. The deferral is structural: Magma slots into 4.3 alongside the SQLite/OpenSSL/libxml2 live-lib runs, which share the OSS-Fuzz base image and per-target build patterns. (b) **`candidate` is excluded from the confirmation set.** PLAN §8 says symbolic SAT alone never replaces the runtime checker, and the Phase 2.4 router enforces this structurally (Tier-2 SAT becomes `confirmed` only after a Tier-1 reconfirm). The precision corpus carries two Tier-2 SAT cases (`klee_sat_div_by_zero`, `angr_magic`) that are buggy in the symbolic-feasibility sense but lack a Tier-1 reconfirm spec — they correctly hold at `candidate`. Counting `candidate` as a confirmation would (i) inflate the precision/recall headline by treating informational verdicts as TPs, and (ii) hide the false-confirmation surface that *only* `confirmed/bmc_unsafe` represents. The corpus's six "buggy" labels split 4/2 between `confirmed-or-bmc_unsafe` (TPs) and `candidate` (held-pending-reconfirm); the recall of 1.0 means "every buggy case whose expected outcome was a confirmation actually got confirmed", not "every buggy case was confirmed". (c) **The corpus is intentionally tier-spanning and small (11 hypotheses, 16 s wall) rather than throughput-heavy.** Phase 2's gate is *precision* (zero false confirmations + zero soundness violations + zero missed buggy cases), not coverage. Throughput coverage is Phase 3.4 (CyberGym 10-task ablation) and Phase 4 (kernelCTF + live-lib). Smoke runtimes are dominated by the Tier-1 `clean_input_handler` 10 s wall budget (the negative case where libFuzzer correctly fails to find a bug) and CBMC's ~600 ms-per-property runs; everything else is sub-second.
- 2026-05-27: Phase 2.3 complete — Tier-3 BMC oracle stood up under one `Tier3Verdict` schema mirroring Stage B; three deterministic CBMC smokes pass (safe/unsafe/inconclusive) with PoV-extraction validated on the unsafe smoke. Three decisions logged: (a) **engine is shared with Stage B** but the Tier-3 wrapper calls CBMC directly rather than going through `surface.stage_b.run_cbmc` — Stage B compresses output to the last 12 lines (fine for a proof-cache key) but a cex trace needs the full body (~80 non-empty lines) to extract input-variable assignments. The two callers share the regexes (`_CBMC_OK`, `_CBMC_UNWIND_FAIL`, `_CBMC_VIOLATED`) imported from `surface.stage_b` so the verdict-classification logic stays in one place. (b) **`--unwinding-assertions` is forced ON, not configurable**. PLAN §3 Tier-3 wants definite yes/no answers — silently allowing a loop to exceed the bound and reporting `safe` would be the worst kind of soundness leak (false negative in an offensive search). Encoded structurally in the driver, recorded in `docs/soundness-assumptions.md` as a non-negotiable Tier-3 invariant. The cost is more `inconclusive` verdicts on unbounded loops, which Phase 3.1 will fix via LLM-synthesized loop invariants — the right place for that fix. (c) **Symbolic inputs as uninitialized locals**, not `__CPROVER_nondet_*()`. Initial smokes failed with "no body for callee __CPROVER_nondet_uint" because CBMC requires extern decls for those builtins and the supported suffix set is unstable across versions. Uninitialized locals are CBMC's canonical nondet idiom, version-stable, and the same trace extraction works because CBMC still emits `i=8u` in the trace. Recorded in `docs/soundness-assumptions.md` Tier-3 harness section. **ESBMC ships as a dispatch-uniform stub** (image-missing → clean inconclusive, mirrors the Phase-2.2 SymCC/S2E pattern) — CBMC covers all Phase 2.3 obligations and ESBMC activates if Phase 2.5 finds a CBMC-slow Magma case.
- 2026-05-27: Phase 2.1 complete — Tier-1 crash oracle stood up under one `Tier1Verdict` schema (mirrors Stage B), validated on three deterministic crashes. Three decisions logged: (a) use **host clang-14** for userspace builds rather than building the `clang.Dockerfile` image. Host clang-14 matches `docs/toolchain.lock`'s `LLVM_VERSION=14` exactly, so the builds are bit-for-bit equivalent to what the container would produce — building the image would cost ~1.5 GB of disk (we're at 113 GB free / 94% used) for zero ABI difference. The driver detects the missing `-lstdc++` search path on host Ubuntu (libstdc++-NN-dev puts the .so symlink in `/usr/lib/gcc/x86_64-linux-gnu/<ver>/`, not in the default linker search path) and adds it automatically; the container path is a no-op there. (b) Driver default is **`-O0`** for hand-written harnesses. At `-O1`+ clang folds short if-ladders in tiny harnesses, which collapses libFuzzer's coverage feedback to a single edge — we saw exactly this on the first run (cov stayed at 1 ft 1 for 63M execs / 30s, never converged to the crash). At `-O0` the synthetic harness crashes in 192 ms deterministically. Real OSS-Fuzz binaries (used through the `replay-docker` path) come pre-built with `-O1`+ and don't suffer this — they have enough structural code to keep coverage useful. The driver exposes `extra_cflags` to override per harness. (c) **No-crash within budget is `inconclusive`, never `safe`** — encoded in the verdict-construction logic and in the `soundness_note` field of every Tier1Verdict. This is PLAN §3 Tier-1 rule made structural: only Stage B / Tier-3 BMC may emit `safe`. The router (Phase 2.4) will key escalation off this exact distinction. **Syzkaller-as-fuzzer is deferred to Phase 4.2** — PLAN explicitly lists kernelCTF as a field target hunted with syzkaller in Phase 4; Phase 2.1 only requires the path be wired (kasan-replay + syzlang descriptor + image-presence smoke), which it is. Building the syzkaller Go-toolchain Docker image (~2 GB) is held until Phase 4.2 needs it.
- 2026-05-26: Phase 1.2 complete — Stage A reachability/taint over Linux 6.1.72 `net/netfilter/` produces a sound over-approximated slice (keep=3029/3886, 22.1% pruned) in 0.5 s. Three decisions logged: (a) the indirect-call soundness lever is "fold all 1221 address-taken funcs into keep_set whenever any reachable fn does an indirect call" — coarser than SVF/CodeQL type-aware resolution but the same guarantee; refining via SVF is a Phase-3 hook (PLAN §6 Phase 3 explicitly puts the LLM-acceleration work there and SVF resolution slots alongside it because both require LLVM bitcode of the kernel, which we don't yet need on disk). (b) Static-analyzer findings (smatch/sparse) are ingested *only as priority hints*, never as pruning evidence — recorded in soundness-assumptions.md so any future change to `static_hints.py` cannot accidentally turn them into a soundness lever. (c) Entry-point detector is allowlist-based over a fixed `DISPATCHER_TYPES` map — the *soundness lever* is adding a new dispatcher type, and `docs/soundness-assumptions.md` now states this explicitly. Soundness sanity: all 9 CVE-2024-1086 critical-path sites in keep_set ✓. Next: 1.3 Stage B Frama-C/EVA + CBMC on the slice with fixed contracts. Note: 37 entry funcs are *out-of-scope* (their definitions live outside `net/netfilter/`, e.g. SCTP/UDP helpers) — these flag a future scope-widening when Stage B chases cross-subsystem contracts; for now they don't affect soundness because their callees inside `net/netfilter/` *are* reached transitively from the entry funcs that *are* in scope.
